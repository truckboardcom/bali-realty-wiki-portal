# Wiki Portal - Рейтинг проектов недвижимости на Бали

Современный PHP сайт-портал в стиле Wiki для отображения рейтинга проектов недвижимости на Бали.

## 🚀 Возможности

- **Адаптивный дизайн** - отлично работает на всех устройствах
- **Боковая навигация** - удобное меню и содержание
- **Структурированный контент** - иерархическая организация информации
- **Поиск по контенту** - быстрый поиск по всем разделам
- **Красивый UI** - современный дизайн с градиентами и анимациями
- **Wiki-стиль** - привычная структура как в Wikipedia

## 📁 Структура проекта

```
bali-realty-portal/
├── index.php           # Главный файл
├── assets/
│   ├── style.css      # Стили
│   └── script.js      # JavaScript
├── pages/
│   ├── home.php       # Главная страница
│   ├── criteria.php   # Критерии оценки
│   ├── rating.php     # Рейтинг проектов
│   └── about.php      # О портале
└── data/
    └── content.json   # Контент из Google Docs
```

## 🛠️ Установка

### Требования

- PHP 7.4 или выше
- Веб-сервер (Apache/Nginx)
- Поддержка JSON

### Шаг 1: Загрузка файлов

Распакуйте архив в директорию вашего веб-сервера:

```bash
# Для Apache (XAMPP/WAMP)
C:/xampp/htdocs/bali-realty-portal/

# Для Nginx
/var/www/html/bali-realty-portal/

# Для локального PHP сервера
/path/to/your/project/
```

### Шаг 2: Настройка прав доступа (Linux/Mac)

```bash
chmod -R 755 bali-realty-portal/
chmod -R 644 bali-realty-portal/data/content.json
```

### Шаг 3: Запуск

**Вариант А: Встроенный PHP сервер (для тестирования)**

```bash
cd bali-realty-portal
php -S localhost:8000
```

Откройте: http://localhost:8000

**Вариант Б: Apache/Nginx**

1. Скопируйте файлы в директорию веб-сервера
2. Откройте: http://localhost/bali-realty-portal

**Вариант В: XAMPP/WAMP**

1. Скопируйте папку в htdocs/
2. Запустите Apache через панель управления
3. Откройте: http://localhost/bali-realty-portal

## 🎨 Кастомизация

### Изменение цветовой схемы

Откройте `assets/style.css` и измените переменные в начале файла:

```css
/* В .sidebar измените градиент */
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

### Добавление нового контента

Контент хранится в `data/content.json`. Структура:

```json
{
  "sections": [
    {
      "title": "Название раздела",
      "level": 1,
      "content": ["Параграф 1", "Параграф 2"],
      "subsections": [...]
    }
  ]
}
```

### Добавление новых страниц

1. Создайте файл в папке `pages/`, например `newpage.php`
2. Добавьте пункт меню в `index.php`:

```php
<li><a href="index.php?page=newpage"><i class="fas fa-icon"></i> Новая страница</a></li>
```

3. Добавьте case в switch:

```php
case 'newpage':
    include 'pages/newpage.php';
    break;
```

## 🔧 Конфигурация

### Настройка базы данных (опционально)

Если хотите использовать БД вместо JSON:

```php
// config.php
$db_host = 'localhost';
$db_name = 'bali_realty';
$db_user = 'root';
$db_pass = '';

$pdo = new PDO("mysql:host=$db_host;dbname=$db_name", $db_user, $db_pass);
```

## 📱 Мобильная версия

Сайт полностью адаптивен. На мобильных устройствах:
- Боковое меню скрывается
- Появляется кнопка гамбургер-меню
- Контент занимает всю ширину экрана

## 🌐 Деплой на хостинг

### Обычный хостинг (cPanel)

1. Загрузите все файлы через FTP/File Manager
2. Убедитесь, что PHP версии 7.4+
3. Установите права 755 на папки, 644 на файлы

### VPS/Dedicated сервер

```bash
# Клонировать файлы
scp -r bali-realty-portal/ user@server:/var/www/html/

# Настроить Nginx
server {
    listen 80;
    server_name your-domain.com;
    root /var/www/html/bali-realty-portal;
    index index.php;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass unix:/var/run/php/php7.4-fpm.sock;
        fastcgi_index index.php;
        include fastcgi_params;
    }
}
```

## 🔒 Безопасность

- JSON файлы защищены от прямого доступа
- Весь вывод экранируется через `htmlspecialchars()`
- Нет прямого выполнения пользовательского кода
- Параметры URL фильтруются

## 📞 Поддержка

Если возникли проблемы:
- Проверьте версию PHP: `php -v`
- Проверьте права доступа к файлам
- Убедитесь, что JSON файл корректен
- Проверьте логи веб-сервера

## 📄 Лицензия

MIT License - свободное использование

## 🎉 Готово!

Теперь ваш Wiki-портал готов к использованию. Наслаждайтесь!
